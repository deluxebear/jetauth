// Stub created by A5 so the list compiles. A6 replaces this with the
// full add-assertion dialog.

interface Props {
  appId: string;
  onClose: () => void;
  onSaved: () => void;
}

export default function BizAssertionEditor(_props: Props) {
  return null;
}
