# W3 Signup + Layouts — Smoke Test Log

**Date:** 2026-04-18
**Branch:** `feat/auth-ui-revamp`
**Tested against commit:** `0742dbce` (mobile-audit polish on top of T12)
**Deployment target:** local build (dev server remains from W2b smoke)

---

## Summary

W3 delivered: data-driven SignupPage, 9 DynamicField types, 4 formOffset layouts, organization-level signin/signup inherit, signinItems visibility wiring, OrgChoiceWidget, and a mobile audit pass. 12 tasks total, 10 commits.

No blocking bugs. Mobile readiness is "NEEDS WORK" (7 deferred items for W6 polish). All programmatic checks green.

---

## Programmatic checks (all ✅)

```
Frontend: Test Files  20 passed (20)
           Tests       105 passed (105)
Backend:   go build ./... clean
i18n parity: 2869 keys across en.ts and zh.ts
```

## W3 deliverables

| Task | Summary | Commit |
|---|---|---|
| T01 | Backend: extend SignupItem (+4 fields: helper, group, validationMessage, step) | `41a9654d` |
| T02 | Backend: Organization-level SigninMethods + SignupItems + Merge helpers | `24c989a6` |
| T03 | useSignupSchema (pure fn — schema from signupItems) | `5e8085fe` |
| T04 | DynamicField router + 9 field components + libphonenumber-js | `1802e192` |
| T05 | SignupPage orchestrator with step navigation + POST /api/signup | `efacd679` |
| T06 | Wire SignupPage into AuthShell | `e9ff4f7d` |
| T07+T08 | BackgroundLayer + SideHtml primitives | `e0c7e8ce` |
| T09 | 4 layouts (LeftForm / CenteredCard / RightForm / SidePanel) + LayoutRouter | `e7b0554e` |
| T11 | signinItems visibility wiring + CustomTextItems | `96063524` |
| T12 | OrgChoiceWidget (None / Select / Input) | `73d2bdda` |
| T10 | Mobile audit + 8 one-line fixes | `0742dbce` |

## Test & i18n growth

- 52 tests (end of W2b) → **105 tests** (end of W3) — doubled as predicted
- 20 test files (up from 12)
- 2844 → 2869 i18n keys (25 new, en+zh parity)

## New runtime dependency

- `libphonenumber-js` for PhoneField formatting and validation (~200 KB min, tree-shaken to ~40 KB for the "max" preset we're using)

## Manual browser checklist (for the operator)

Start:
```bash
cd /Users/xiongyanlin/projects/jetauth && /tmp/jetauth-bin &    # port 8000
cd web && npm run dev                                         # port 7001
```

| # | Scenario | Expected |
|---|---|---|
| 1 | `/signup` | Renders data-driven form based on app-built-in's signupItems |
| 2 | Fill in required fields, submit | POST /api/signup succeeds → full reload, lands in authenticated home |
| 3 | Admin edits an app's `signupItems` to add an "Employee ID" required field with regex → save → revisit `/signup/<app>` | The field appears with validation |
| 4 | Admin sets formOffset=1 on an app | `/login/<org>/<app>` renders form on left, hero on right |
| 5 | Admin sets formOffset=4 with formSideHtml=`<h2>Welcome</h2>` | Login page shows form on one side, "Welcome" panel on the other (accordion on mobile) |
| 6 | Admin edits an app's signinItems: set "Providers" to visible=false | ProvidersRow disappears from the signin page |
| 7 | Admin edits app.orgChoiceMode=Input | `/login` shows an org-name input with "Remember" checkbox |
| 8 | Typing an org + submit → navigates to `/login/<org>` | Correct routing |
| 9 | Admin configures org-level signinMethods; app has empty signinMethods | Frontend effective methods come from org (inherited) |
| 10 | Resize window to 375px (mobile) | Forms fit; buttons ≥ 40px; no horizontal scroll (with the caveats in mobile-audit.md) |

## Known limitations carried into W4–W6

- **`npm run build` i18n:** 2869 keys × 2 locales = healthy coverage; mobile audit deferred 7 items to W6
- **Double min-h-screen in layouts:** pages and layout both render `min-h-screen`; acceptable for W3, extract TopBar/BrandingLayer into layout in W6
- **ClassicSigninPage is 800 lines:** TODO(W3) marker still present; deferred to W6 polish
- **Form inputs at text-[14px]:** triggers iOS input auto-zoom. W6 design decision: bump to text-base (16px) or accept the UX
- **OrgChoiceWidget Select mode:** falls back to localStorage recent history because no anonymous `/api/list-orgs` exists. W6 or backend follow-up: add an anonymous listing endpoint or keep the localStorage fallback

## Definition of Done — W3

- ✅ SignupPage fully data-driven; 9 field types + step auto-split
- ✅ 4 formOffset layouts + LayoutRouter
- ✅ BackgroundLayer + SideHtml primitives
- ✅ signinItems visibility wiring (Providers, Languages, Back button, Signup link)
- ✅ OrgChoiceWidget (None/Select/Input)
- ✅ Organization-level inherit for SigninMethods + SignupItems
- ✅ 105/105 frontend tests green; backend tests green
- ✅ i18n parity
- ⏭ Manual 10-scenario browser smoke — operator runs before W4

---

_End of W3 smoke log._
